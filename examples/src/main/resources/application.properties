# All Rights Reserved, Copyright ? Paysafe Holdings UK Limited 2026. For more information see LICENSE

# Port the application will run on. Access it at http://localhost:8881
server.port=8881

# ---------------------------------------------------------------
# Paysafe API credentials
# Your API key in format: username:password
# Get these from the Paysafe merchant portal (Integration > API Keys)
paysafe.apiKey=pmle-1120430:B-qa2-0-67f67166-0-302c02143f350b7fad3283a80518c57b828e5c6a066f4ce80214610d159025e600820d86c7df64b21a6b108bbda8

# Your account ID from the Paysafe merchant portal
# Only required if you have multiple accounts under one API key
paysafe.accountId=1002926630

# ---------------------------------------------------------------
# Return URL configuration
# After a payment redirect (e.g. 3DS, PayPal), Paysafe will
# redirect the customer back to this base URL.
# Must match the URL you use to access the app in your browser.
# ---------------------------------------------------------------
paysafe.returnUrlBase=http://127.0.0.1:8881

# Disable URL-encoded session tracking (prevents ;jsessionid=... in redirect URLs).
# Without this, Tomcat appends jsessionid to the 3DS return redirect when cookies
# are absent, which breaks the Thymeleaf model and causes a 500 error.
server.servlet.session.tracking-modes=COOKIE


